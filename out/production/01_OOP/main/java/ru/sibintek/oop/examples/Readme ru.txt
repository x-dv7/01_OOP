# Объектно-ориентированное программирование на Java
🚀 Полный курс Java от А до Я.

Ссылки: [Geeksforgeeks](https://www.geeksforgeeks.org/java/) , [javabeginnerstutorial](https://javabeginnerstutorial.com/core-java-tutorial/)


**JAVA - ЭТО ЯЗЫК, НЕ ЗАВИСЯЩИЙ ОТ ПЛАТФОРМЫ**

Коды, написанные на Java, преобразуются в язык промежуточного уровня, называемый байт-кодом, который после компиляции становится частью платформы Java, независимо от машины, на которой выполняется код.Это делает Java очень переносимым, поскольку его байт-коды могут быть запущены на любой машине интерпретатором, называемым **Java Virtual Machine (JVM).**



# CLASS:

Класс - это определенный пользователем шаблон или прототип, на основе которого создаются объекты.  
Он представляет собой набор свойств или методов, общих для всех объектов одного типа. В общем, объявления классов могут включать эти компоненты, чтобы:

    Модификаторы: Класс может быть общедоступным или иметь доступ по умолчанию.
    
    Имя класса: Название должно начинаться с начальной буквы (по соглашению, с заглавной).
    
    Суперкласс (если таковой имеется): имя родительского класса (суперкласса), если таковое имеется, которому предшествует ключевое слово extends. Класс может расширять (подклассифицировать) только одного родительского класса.
    
    Интерфейсы (если таковые имеются): разделенный запятыми список интерфейсов, реализованных классом, если таковые имеются, перед которым указывается ключевое слово implements. Класс может реализовывать более одного интерфейса.
    
    Тело: Тело класса, заключенное в фигурные скобки { }.

# OBJECT:

Объект - это экземпляр класса.Технически, класс - это шаблон, который описывает, какое состояние и поведение экземпляра может иметь этот класс. 
Объект реализует состояние и поведение в виде переменных и методов и требует выделения некоторой памяти.Объект состоит из:

    Состояние: Оно представлено атрибутами объекта. Оно также отражает свойства объекта.
    
    Поведение: Оно представлено методами объекта. Оно также отражает реакцию объекта на другие объекты.
    
    Идентификация: присваивает объекту уникальное имя и позволяет одному объекту взаимодействовать с другими объектами.
    
    
**Объявление объектов (также называемое созданием экземпляра класса):**

Когда создается объект класса, класс называется созданным экземпляром. 
Все экземпляры имеют общие атрибуты и поведение класса. Но значения этих атрибутов, т.е. состояние, уникальны для каждого объекта. 
Один класс может иметь любое количество экземпляров.




# МЕТОДЫ:

Метод - это набор инструкций, которые при вызове выполняют определенные задачи и возвращают результат вызывающей стороне. Метод может выполнять определенные задачи, даже ничего не возвращая. Методы позволяют нам повторно использовать код без повторного ввода кода.

**Объявление метода:**
Удаление метода содержит шесть компонентов:

   1.**Модификатор:** Определяет тип доступа к методу, т.е. откуда к нему можно получить доступ в вашем приложении. В Java существует 4 типа спецификаторов доступа.
   
        public: доступный во всех классах вашего приложения.
        защищенный: доступен в пределах класса, в котором он определен, и в его подклассе(ах)
        закрытый: доступен только в пределах класса, в котором он определен.
        по умолчанию (объявлен/определен без использования каких-либо модификаторов): доступен в пределах того же класса и пакета, в котором определен его класс.
  2.**Тип возвращаемого значения:** Тип данных значения, возвращаемого методом, или void, если значение не возвращается.
   
  3.**Имя метода:** правила для имен полей применяются также к именам методов, но соглашение немного отличается.
   
  4.**Список параметров:** В круглых скобках, разделенных запятыми, указан список входных параметров, которым предшествует их тип данных. Если параметров нет, используются пустые круглые скобки ().
  
  5.**Список исключений:** Можно указать исключения, которые может генерировать метод.
  
  6.**Тело метода:** Оно заключено в фигурные скобки {} и содержит код, который должен быть выполнен для выполнения запланированных операций.

**Выделение памяти для вызовов методов:**

Вызовы методов реализуются через стек. Всякий раз, когда вызывается метод, в области стека создается стековый фрейм, и после этого передаваемые аргументы, а также локальные переменные и значение, которые должны быть возвращены этим вызываемым методом, сохраняются в этом стековом фрейме, и когда выполнение вызываемого метода завершается, выделенный стековый фрейм будет удален. Существует регистр указателя стека, который отслеживает вершину стека и настраивается соответствующим образом.

t-Oriented-Programming-in-Java/blob/master/Methods/PassingObjects1.java):** Программа, показывающая, что ссылки также передаются по значению. 

Если мы не меняем ссылку на какой-либо другой объект (или ячейку памяти), мы можем внести изменения в элементы, и эти изменения будут отражены обратно.

Когда мы передаем примитивный тип методу, он передается по значению. Но когда мы передаем объект методу, ситуация кардинально меняется, потому что объекты передаются по принципу "вызов по ссылке". Java использует что-то вроде гибрида между передачей по значению и передачей по ссылке. В принципе, функция не может изменить параметр, но функция может попросить параметр изменить себя, вызвав какой-либо метод внутри себя.

Создавая переменную типа class, мы создаем только ссылку на объект. Таким образом, когда мы передаем эту ссылку методу, принимающий ее параметр будет ссылаться на тот же объект, на который ссылается аргумент.Это фактически означает, что объекты действуют так, как если бы они передавались методам с помощью вызова по ссылке.Изменения в объекте внутри метода отражаются в объекте, используемом в качестве аргумента.



### ПЕРЕГРУЗКА МЕТОДА:

В Java можно определить два или более метода в пределах одного класса, которые имеют одинаковое имя, при условии, что их объявления параметров отличаются либо количеством параметров, либо типом параметров, либо и тем, и другим.Этот процесс называется перегрузкой метода.
С точки зрения приоритета, компилятор выполняет эти шаги, когда точный прототип не соответствует аргументам:


1.Преобразование типа, но к более высокому типу (с точки зрения диапазона) в том же семействе.
2.Преобразование типа в следующее более высокое семейство (предположим, если для типа данных int нет доступного типа данных long, то будет выполнен поиск типа данных float).

Мы можем перегрузить два или более статических метода с одинаковыми именами, но отличающимися входными параметрами.Мы не можем перегрузить два метода в Java, если они отличаются только ключевым словом static (количество параметров и типы параметров одинаковы). 

Компилятор не учитывает тип возвращаемого значения при дифференциации перегруженного метода. Но два метода с одинаковой сигнатурой и разным типом возвращаемого значения не могут быть объявлены. Это приведет к ошибке во время компиляции.

Следовательно, перегрузка метода может быть выполнена путем изменения:

    1.Количества параметров в двух методах.
    2.Типов данных параметров методов.
    3.Порядок следования параметров методов.
    


### ПЕРЕОПРЕДЕЛЕНИЕ МЕТОДА:

Когда метод в подклассе имеет то же имя, то же объявление параметров и тот же тип возвращаемого значения (или подтип подтипа), что и метод в его суперклассе, тогда считается, что метод в подклассе переопределяет метод в суперклассе.
 Модификатор доступа для переопределяющего метода может предоставлять больший, но не меньший доступ, чем переопределяемый метод. Например, защищенный метод экземпляра в суперклассе может быть общедоступным, но не закрытым в подклассе. Это приведет к возникновению ошибки во время компиляции.

Частные методы нельзя переопределить, поскольку они связаны во время компиляции. Поэтому мы не можем переопределить частные методы даже в подклассе.

Для переопределяющего метода в дочернем классе может быть другой тип возвращаемого значения, но тип возвращаемого значения дочернего класса должен быть подтипом типа возвращаемого значения родительского класса. Это называется ковариантным типом возвращаемого значения.


# КОНСТРУКТОРЫ:

Конструкторы используются для инициализации новых объектов. Функция Constructors не возвращает никаких значений, но неявно возвращает объект класса. Поля - это переменные, которые определяют состояние класса и его объектов, а методы используются для реализации поведения класса и его объектов.



**Конструкторы не могут быть унаследованы:**

Когда класс расширяет другой класс, дочерний класс наследует переменные, методы и поведение суперкласса, но не конструкторы.Конструкторы имеют то же имя, что и имя класса. Таким образом, если бы конструкторы были унаследованы от дочернего класса, то дочерний класс содержал бы конструктор родительского класса, что противоречит ограничению, согласно которому конструктор должен иметь то же имя, что и имя класса.

**Конструкторы по умолчанию:**

Java автоматически создает конструктор по умолчанию, если пользователь не создал конструктор по умолчанию или параметризованный конструктор.Конструктор по умолчанию в Java инициализирует переменные данных-членов значениями по умолчанию


**На что следует обратить внимание:**

    1.Выражение this() всегда должно быть первой строкой конструктора.
    2.Должен быть хотя бы один конструктор без ключевого слова this().
    3.Объединение конструкторов в цепочки может быть выполнено в любом порядке.

### ЧАСТНЫЕ КОНСТРУКТОРЫ И ОДНОЭЛЕМЕНТНЫЙ КЛАСС:

Мы можем предоставить конструктору спецификатор доступа. Если сделать его закрытым, то доступ к нему будет возможен только внутри класса.
Мы можем использовать закрытые конструкторы для 1. Внутренней цепочки конструкторов 2. Разработки одноэлементного класса

Одноэлементный класс - это класс, который может содержать не более одного объекта.
После первого раза, если мы попытаемся создать экземпляр класса Singleton, новая переменная также укажет на первый созданный экземпляр. Таким образом, какие бы изменения мы ни вносили в переменную внутри класса с помощью любого экземпляра, это влияет на переменную единственного созданного экземпляра и становится видимым, если мы обращаемся к этой переменной с помощью любого экземпляра этого класса.


Для создания одноэлементного класса:

    1.Сделайте конструктор закрытым.
    2.Напишите статический метод, который возвращает тип object этого одноэлементного класса. Здесь для написания этого статического метода используется концепция отложенной инициализации.

## КОНСТРУКТОРЫ ПРОТИВ МЕТОДОВ:

Конструкторы в Java можно рассматривать как методы в классе. Но между конструкторами и методами есть большая разница.
